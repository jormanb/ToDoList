<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <main>
        <div class="main">
            <h1 class="title">ToDoList</h1>
            <h2 class="date"><%= Fecha %></h2>
            <div class="list">
                <form action="/erase" method="post">
                <button class="deleteBtn" type="submit">Eliminar Tareas Completadas</button>
                <ul>
                    <% for(i = 0; i<Lista.length; i++){%>
                        <li class="item">
                            <input class="checkbox" type="checkbox" name=<%=i%>>
                            <span class="text"><%= Lista[i] %></span>
                        </li>
                    <% } %>
                </ul>
                </form>
                <form class="newElement" action="/add" method="post">
                    <input class="newItem" type="text" name="newItem" placeholder="Nuevo Elemento..." autocomplete="off">
                    <button class="addBtn" type="submit" name="button">Añadir</button>
                </form>
            </div>
        </div>
    </main>

    
</body>
</html>